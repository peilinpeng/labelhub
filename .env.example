# LabelHub Docker Compose 环境变量模板
# 用法：cp .env.example .env；只在本机 .env 中填写真实密钥，禁止提交。
# 完整必填项、默认值与敏感等级见 docs/environment-variables.md。

# ── 运行模式 ──────────────────────────────────────────────────
APP_ENV=demo
DEMO_MODE=true
LABELHUB_IMAGE_TAG=local

# ── 数据库 / Redis ────────────────────────────────────────────
DATABASE_URL=mysql+pymysql://labelhub:labelhub@mysql:3306/labelhub
REDIS_URL=redis://redis:6379
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=labelhub
MYSQL_USER=labelhub
MYSQL_PASSWORD=labelhub

# ── 鉴权与安全 ────────────────────────────────────────────────
# 生产环境请使用 `openssl rand -hex 32`，且不得保留此示例值。
JWT_SECRET=change_me_to_a_long_random_secret
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=10080
LOGIN_RATE_LIMIT_BACKEND=memory
LOGIN_RATE_LIMIT_ATTEMPTS=5
LOGIN_RATE_LIMIT_WINDOW_SECONDS=300
TRUSTED_HOSTS=localhost,127.0.0.1,testserver,api
ENABLE_HSTS=false

# ── 豆包 / OpenAI 兼容接口 ────────────────────────────────────
DOUBAO_API_KEY=
DOUBAO_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
DOUBAO_MODEL=
AI_REVIEW_CONFIDENCE_THRESHOLD=0.6

# ── 文件存储 ──────────────────────────────────────────────────
FILE_STORAGE_DRIVER=local
LOCAL_STORAGE_DIR=/data/files
MAX_UPLOAD_SIZE_BYTES=20971520
FILE_ALLOWED_EXTENSIONS=.csv,.json,.jsonl,.txt,.pdf,.png,.jpg,.jpeg,.webp,.xlsx,.xls
FILE_ALLOWED_MIME_TYPES=text/csv,application/json,application/x-ndjson,text/plain,application/pdf,image/png,image/jpeg,image/webp,application/vnd.openxmlformats-officedocument.spreadsheetml.sheet,application/vnd.ms-excel

# ── 宿主端口 ──────────────────────────────────────────────────
WEB_PORT=5173
API_PORT=3000
# 仅 docker-compose.dev.yml 使用；默认 Compose 不发布 MySQL / Redis 端口。
MYSQL_PORT=3306
REDIS_PORT=6379

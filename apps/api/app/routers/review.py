# 人工审核路由，对应契约第 23.5 节。
# 端点：GET /review/queue、GET /review/submissions/:submissionId、
#   POST /review/submissions/:submissionId/claim、
#   POST /review/submissions/:submissionId/decision、
#   POST /review/batch-decision。
